这份更新后的技术文档包含了对 run\_longExp.py 参数配置的修改，确保您可以直接通过命令行调整正则化参数。

# ---

**FreTS 模型频域正则化与自适应稀疏改进技术方案 (v2.0)**

## **1\. 改进背景与动机**

现有的 FreTS 模型主要利用 MLP 在频域提取特征，并通过时域重构误差（MSE/MAE）进行优化 1111。然而，仅依赖时域损失存在以下局限性：

* **高频噪声敏感性不足**：时域 MSE 往往关注整体波形拟合，容易忽略高频部分的抖动（噪声），导致预测结果不够平滑。  
* **频域结构约束缺失**：模型可能生成在时域看似合理，但频谱分布与真实物理规律不符的信号 2。

* **固定阈值的局限**：原模型通常使用固定的稀疏阈值（如 hard-coded 0.01），难以适应不同数据集或不同频段的信号特性。

本方案提出两项核心改进：

1. **引入可微频域正则化损失 (Differentiable Frequency Regularization Loss)**：通过卷积模拟小波变换，在保持梯度可导的前提下，约束预测信号的频域分布 3。

2. **自适应频域稀疏阈值 (Adaptive Frequency Sparsity)**：将固定的去噪阈值转化为可学习参数，让模型根据数据自动学习“该过滤多少噪声” 4。

## ---

**2\. 改进模块一：可微频域正则化损失 (FrequencyLoss.py)**

### **2.1 技术原理**

为了避免使用 pywt (PyWavelets) 等库导致的梯度断裂问题，我们利用 **卷积神经网络 (Conv1d)** 的数学等价性来实现离散小波变换 (DWT) 5。

* **原理**：小波分解等价于 stride=2 的低通和高通滤波器卷积操作 6。

* **策略**：  
  * **低频一致性**：要求预测信号的趋势（低频近似）与真实值高度一致。  
  * **高频稀疏/去噪**：对高频细节进行能量约束，防止产生不切实际的高频噪声 7777。

### **2.2 代码实现**

请新建文件 FrequencyLoss.py 并写入以下代码：

Python

import torch  
import torch.nn as nn  
import torch.nn.functional as F  
import numpy as np

class WaveletConv1d(nn.Module):  
    """  
    使用 Conv1d 模拟单层离散小波变换 (DWT)，确保梯度可导。  
    支持 Haar 小波 (默认) 或 db2 小波。  
    """  
    def \_\_init\_\_(self, in\_channels, wavelet\_name='haar'):  
        super(WaveletConv1d, self).\_\_init\_\_()  
          
        \# 1\. 定义小波滤波器系数  
        if wavelet\_name \== 'haar':  
            c \= 1 / np.sqrt(2)  
            dec\_lo \= \[c, c\]      \# 低通 (Approximation)  
            dec\_hi \= \[-c, c\]     \# 高通 (Detail)  
            k\_size \= 2  
            padding \= 0  
        elif wavelet\_name \== 'db2':  
            dec\_lo \= \[-0.12940952255092145, 0.22414386804185735, 0.836516303737469, 0.48296291314469025\]  
            dec\_hi \= \[-0.48296291314469025, 0.836516303737469, \-0.22414386804185735, \-0.12940952255092145\]  
            k\_size \= 4  
            padding \= 1  
        else:  
            raise ValueError("Unsupported wavelet type. Try 'haar' or 'db2'.")

        \# 2\. 构建卷积核权重 \[out, in, k\]  
        dec\_lo \= torch.tensor(dec\_lo, dtype=torch.float32).flip(0).view(1, 1, \-1)  
        dec\_hi \= torch.tensor(dec\_hi, dtype=torch.float32).flip(0).view(1, 1, \-1)

        \# 注册为 buffer (不更新这些权重)  
        self.register\_buffer('filter\_lo', dec\_lo)  
        self.register\_buffer('filter\_hi', dec\_hi)  
          
        self.in\_channels \= in\_channels  
        self.stride \= 2  
        self.padding \= padding

    def forward(self, x):  
        """Input: \[Batch, Channel, Length\], Output: (Approximation, Detail)"""  
        weight\_lo \= self.filter\_lo.repeat(self.in\_channels, 1, 1)  
        weight\_hi \= self.filter\_hi.repeat(self.in\_channels, 1, 1)

        x\_approx \= F.conv1d(x, weight\_lo, stride=self.stride, padding=self.padding, groups=self.in\_channels)  
        x\_detail \= F.conv1d(x, weight\_hi, stride=self.stride, padding=self.padding, groups=self.in\_channels)  
        return x\_approx, x\_detail

class FrequencyRegularizedLoss(nn.Module):  
    def \_\_init\_\_(self, reg\_lambda=0.01, in\_channels=1, wavelet='haar'):  
        super().\_\_init\_\_()  
        self.base\_loss \= nn.MSELoss()  
        self.reg\_lambda \= reg\_lambda  
        self.dwt \= WaveletConv1d(in\_channels=in\_channels, wavelet\_name=wavelet)

    def forward(self, y\_pred, y\_true):  
        \# 维度调整：确保输入为 \[Batch, Channel, Length\]  
        if y\_pred.dim() \== 3 and y\_pred.shape\[-1\] \== self.dwt.in\_channels:  
            y\_pred\_t \= y\_pred.permute(0, 2, 1)  
            y\_true\_t \= y\_true.permute(0, 2, 1)  
        else:  
            y\_pred\_t \= y\_pred  
            y\_true\_t \= y\_true

        loss\_time \= self.base\_loss(y\_pred, y\_true)

        pred\_a, pred\_d \= self.dwt(y\_pred\_t)  
        true\_a, true\_d \= self.dwt(y\_true\_t)

        loss\_low \= F.mse\_loss(pred\_a, true\_a)  
        loss\_high \= F.mse\_loss(pred\_d, true\_d) \# 混合策略：高频分布匹配

        loss\_freq \= loss\_low \+ loss\_high  
        return loss\_time \+ self.reg\_lambda \* loss\_freq, loss\_time, loss\_freq

## ---

**3\. 改进模块二：自适应稀疏阈值 (FreTS.py)**

### **3.1 改进说明**

在 FreTS.py 的 Model 类中，将原本固定的稀疏阈值改为可学习参数 nn.Parameter，使模型能自适应地过滤噪声 8。

### **3.2 代码修改**

打开 FreTS.py，定位到 Model 类：

**步骤 1：在 \_\_init\_\_ 中初始化**

Python

class Model(nn.Module):  
    def \_\_init\_\_(self, configs):  
        super(Model, self).\_\_init\_\_()  
        \# ... (原有代码) ...  
          
        \# \=== \[新增\] 自适应稀疏阈值 \===  
        \# 初始化为 0.01，设为 Parameter 以便学习  
        self.sparsity\_threshold \= nn.Parameter(torch.tensor(0.01))

**步骤 2：在 FreMLP 中应用**

Python

    def FreMLP(self, B, N, L, x, r, i, rb, ib):  
        \# ... (原有计算 o1\_real, o1\_imag) ...  
          
        \# \=== \[新增\] 应用自适应软阈值 \===  
        y \= torch.stack(\[o1\_real, o1\_imag\], dim=-1)  
          
        \# 确保阈值非负  
        threshold \= F.relu(self.sparsity\_threshold)  
        y \= F.softshrink(y, lambd=threshold)  
          
        y \= torch.view\_as\_complex(y)  
        return y

## ---

**4\. 改进模块三：参数配置与集成 (run\_longExp.py & exp\_main.py)**

### **4.1 注册命令行参数 (run\_longExp.py)**

为了灵活控制实验，我们需要在 run\_longExp.py 中注册新参数，避免硬编码。

打开 run\_longExp.py，在 if \_\_name\_\_ \== '\_\_main\_\_': 块中的 argparse 部分添加：

Python

    \# ... (原有参数) ...  
    parser.add\_argument('--d\_model', type\=int, default=512, help\='dimension of model')

    \# \=== \[新增\] 频域正则化参数 \===  
    parser.add\_argument('--reg\_lambda', type\=float, default=0.01, help\='frequency regularization weight')  
    parser.add\_argument('--wavelet', type\=str, default='haar', help\='wavelet type: haar or db2')  
    \# \==========================

    parser.add\_argument('--n\_heads', type\=int, default=8, help\='num of heads')  
    \# ...

### **4.2 集成 Loss (exp\_main.py)**

打开 exp\_main.py，修改 \_select\_criterion 方法以使用上述参数。

**步骤 1：引入模块**

Python

from FrequencyLoss import FrequencyRegularizedLoss

**步骤 2：修改选择逻辑**

Python

    def \_select\_criterion(self):  
        \# 使用 self.args 获取命令行参数  
        criterion \= FrequencyRegularizedLoss(  
            reg\_lambda=self.args.reg\_lambda,  
            in\_channels=self.args.enc\_in,  
            wavelet=self.args.wavelet  
        )  
        return criterion

## ---

**5\. 运行与验证**

完成上述修改后，您可以通过命令行启动实验并调整参数：

**默认运行 (Haar 小波, 权重 0.01):**

Bash

python \-u run\_longExp.py \--model\_id ETTm1\_96\_96 \--model FreTS \--data ETTm1

**调整正则化强度与小波基:**

Bash

python \-u run\_longExp.py \\  
  \--model\_id ETTm1\_96\_96 \\  
  \--model FreTS \\  
  \--data ETTm1 \\  
  \--reg\_lambda 0.05 \\  
  \--wavelet db2

## **6\. 预期效果**

* **泛化提升**：reg\_lambda 将约束模型不过拟合高频噪声。  
* **自适应去噪**：可学习的 sparsity\_threshold 将根据数据特征自动调整去噪力度。  
* **实验灵活**：通过命令行参数可快速进行消融实验（Ablation Study）。